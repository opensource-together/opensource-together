import GithubCallbackView from "@/features/auth/views/github-callback.view";

/**
 * Renders the GitHub OAuth callback page.
 *
 * Displays the view responsible for handling the GitHub authentication callback.
 */
export default function GithubCallbackPage() {
  return <GithubCallbackView />;
}
